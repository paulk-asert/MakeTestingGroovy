def cukeVersion = '1.2.5'

dependencies {
    testCompile "info.cukes:cucumber-java:${cukeVersion}"
    testCompile "info.cukes:cucumber-junit:${cukeVersion}"
    testCompile project(':HtmlUnitDslBasic')
}

test {
    testLogging {
        events "PASSED", "FAILED", "SKIPPED"
    }
}
