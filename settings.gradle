def subprojects = [
        'AppUnderTest',
        'HtmlUnit',
        'HttpBuilder',
        'QuickCheck',
        'Vanilla'
]
subprojects.each {
    include it
}

rootProject.name = 'MakeTestingGroovy'
