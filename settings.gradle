def subprojects = [
        'AppUnderTest',
        'DataDriven',
        'EasyB',
        'HtmlUnit',
        'HttpBuilder',
        'QuickCheck',
        'Spock',
        'Vanilla'
]
subprojects.each {
    include it
}

rootProject.name = 'MakeTestingGroovy'
