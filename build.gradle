plugins {
    id 'com.github.ben-manes.versions' version '0.15.0'
}

allprojects {
    apply plugin: 'groovy'
    apply plugin: 'idea'
    apply plugin: 'eclipse'
    repositories {
        jcenter()
    }
}

idea {
    project {
        vcs = 'Git'
    }
}
