ext.groovyVersion = '2.4.11'
allprojects {
    apply plugin: 'groovy'
    apply plugin: 'idea'
    apply plugin: 'eclipse'
    repositories {
        jcenter()
    }
}

idea {
    project {
        vcs = 'Git'
    }
}
